import PageLoader from '~/components/loaders/page-loader';

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <PageLoader />; //<OutlineIcon name="world" size={50} />;
}
